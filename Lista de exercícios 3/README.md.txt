# 📘 Lista de Exercícios 3 - Linguagens de Programação

**Disciplina:** Linguagens de Programação
**Professor:** Fabrício Rodrigues Inácio

---

## 📂 Estrutura do Projeto

Cada exercício foi resolvido em um arquivo separado, numerado de `01` a `06`, dentro desta mesma pasta.


## 📝 Lista de Exercícios

1. **Divisão com tratamento de exceção**
   Implemente um programa que peça dois números inteiros e tente realizar uma divisão.

   * Trate a exceção de **divisão por zero** utilizando `try-catch`.

2. **Entrada inválida do usuário**
   Peça ao usuário para digitar um número.

   * Trate a exceção caso o usuário insira algo que **não seja um número**.

3. **Índice fora do limite**
   Crie um programa que tente acessar um índice fora do limite de um array.

   * Trate a exceção adequadamente.

4. **Exceção personalizada - IdadeInvalidaException**
   Crie uma classe de exceção personalizada chamada `IdadeInvalidaException`.

   * Implemente um programa que peça a idade de uma pessoa.
   * Lance essa exceção se a idade for **negativa** ou **maior que 150**.

5. **Raiz quadrada com IllegalArgumentException**
   Crie uma função que calcula a raiz quadrada de um número.

   * Lance uma `IllegalArgumentException` se o número for **negativo**.
   * A exceção deve ser capturada e tratada no `main`.

6. **Tratamento de múltiplas exceções**
   Crie um programa que tente dividir dois números e realizar operações com um array.

   * Trate as múltiplas exceções usando `try-catch`.

---
