# 📘 Algoritmos de Ordenação: Inserção e Heap Máximo

**Disciplina:** Linguagens de Programação
**Professor:** Fabrício Rodrigues Inácio

---

## 📂 Estrutura do Projeto

Este programa implementa dois algoritmos distintos de manipulação de vetores:

* **Ordenação por Inserção (Insertion Sort)**
* **Construção de Heap Máximo (Max Heap)**

O código está contido no arquivo `entrega.java`.

---

## 📝 Descrição dos Algoritmos

### 1. **Ordenação por Inserção (Insertion Sort)**

* Percorre o vetor a partir da segunda posição.
* Para cada elemento, compara com os anteriores e desloca-os até encontrar a posição correta.
* Insere o elemento na posição ordenada.
* Resultado: vetor ordenado em ordem crescente.

### 2. **Construção de Heap Máximo (Max Heap)**

* A função `aplicarHeap` garante a propriedade de **heap máximo**, comparando o nó pai com seus filhos e trocando se necessário.
* A função `construirHeap` percorre o vetor de baixo para cima, aplicando o ajuste em cada subárvore.
* Resultado: vetor transformado em um **heap binário máximo**, onde cada pai é maior que seus filhos.

---

## 🔑 Conceitos Reforçados

* Estruturas de repetição (`for`, `while`).
* Uso de vetores dinâmicos definidos pelo usuário.
* Implementação de algoritmos de ordenação clássicos.
* Manipulação de estruturas de dados baseadas em **árvores binárias** (heap).
* Diferença entre ordenação direta e construção de estrutura auxiliar (heap).

---
