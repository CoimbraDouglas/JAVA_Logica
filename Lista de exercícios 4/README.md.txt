# 📘 Algoritmos de Ordenação - Linguagens de Programação

**Disciplina:** Linguagens de Programação
**Professor:** Fabrício Rodrigues Inácio

---

## 📂 Estrutura do Projeto

Este projeto contém dois programas independentes, cada um implementando um algoritmo clássico de ordenação:

* `LPEX1.java` → Ordenação por Seleção (Selection Sort)
* `LPEX2.java` → Ordenação por Bolha (Bubble Sort - versão recursiva)

---

## 📝 Descrição dos Programas

### 1. **LPEX1 - Algoritmo de Ordenação por Seleção (Selection Sort)**

* O programa inicializa um vetor de inteiros.
* Utiliza o algoritmo **Selection Sort** para ordenar os elementos:

  * Para cada posição `i`, encontra o menor elemento no subvetor à direita.
  * Realiza a troca entre o elemento mínimo encontrado e o elemento na posição atual.
* Ao final, imprime o vetor já ordenado.

### 2. **LPEX2 - Algoritmo de Ordenação por Bolha (Bubble Sort Recursivo)**

* O programa inicializa um vetor de inteiros e o imprime na forma original.
* Utiliza uma **função recursiva** para aplicar o algoritmo **Bubble Sort**:

  * Percorre o vetor, comparando elementos adjacentes.
  * Troca de posição se estiverem fora de ordem.
  * Repete o processo de forma recursiva até que todo o vetor esteja ordenado.
* Exibe o vetor antes e depois da ordenação.

---

## 🔑 Conceitos Reforçados

* Estruturas de repetição (`for`).
* Uso de **vetores** em Java.
* Implementação de **algoritmos de ordenação clássicos**.
* Diferença entre abordagens **iterativas (Selection Sort)** e **recursivas (Bubble Sort)**.

---
